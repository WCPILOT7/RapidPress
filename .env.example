# Core environment configuration for RapidPress
# Duplicate this file to `.env` and fill in secrets.

# --- Supabase ---
SUPABASE_URL=https://dyugugmrzbervwsevbty.supabase.co
SUPABASE_ANON_KEY=REPLACE_WITH_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SERVICE_ROLE_KEY

# --- OpenAI ---
OPENAI_API_KEY=sk-...

# --- Session / Security (legacy, can be removed after full JWT migration) ---
SESSION_SECRET=change_me_in_production
DISABLE_SESSIONS=0

# --- Quotas & Limits ---
# DISABLE_LIMITS modes:
# 0 / unset  -> нормальный режим (enforced)
# 1          -> полный bypass (нет 429, квоты игнорируются)
# soft       -> мягкий режим (квота не блокирует, rate limit не возвращает 429, но выставляет X-RateLimit-SoftExceeded=1)
FREE_PLAN_MONTHLY_TOKENS=200000
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_CALLS=120
DISABLE_LIMITS=0

# --- Misc ---
NODE_ENV=development
PORT=3000

# Examples:
# DISABLE_LIMITS=1      # полный bypass
# DISABLE_LIMITS=soft   # soft mode (наблюдаем превышения без блокировки)
